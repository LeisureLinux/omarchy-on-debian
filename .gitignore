# never commit the upstream payload or fetched sources
/cache/
*.tar.gz
*.patch.orig
*.rej
.DS_Store

# local backup directories (date-stamped, never shipped)
/.bak-*/

# local article preview renders (the .md source is tracked)
/articles/*.html
